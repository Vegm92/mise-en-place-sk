# ── Database ───────────────────────────────────────────────────────────────────
# Supabase PostgreSQL direct connection (not pooler) — used by Drizzle ORM
# Get from: Supabase → Project Settings → Database → Connection string (URI)
DATABASE_URL=postgresql://postgres:[your-db-password]@db.[project-ref].supabase.co:5432/postgres

# ── Supabase ────────────────────────────────────────────────────────────────────
# Get all values from: Supabase → Project Settings → API
SUPABASE_URL=https://[project-ref].supabase.co
SUPABASE_ANON_KEY=eyJ...          # "anon public" JWT
SUPABASE_SERVICE_ROLE_KEY=eyJ...  # "service_role" JWT — keep secret, server-only

# ── File storage ───────────────────────────────────────────────────────────────
UPLOADS_DIR=uploads

# ── Gemini AI ──────────────────────────────────────────────────────────────────
GEMINI_API_KEY=
# Gemini model — update here when Google deprecates the current one
# https://ai.google.dev/gemini-api/docs/models
GEMINI_MODEL=gemini-2.5-flash

# ── Chat rate limit ────────────────────────────────────────────────────────────
# Requests per minute per IP
CHAT_RATE_LIMIT_RPM=20

# ── Invoice extraction ─────────────────────────────────────────────────────────
MAX_CONCURRENT_EXTRACTIONS=3

# ── Observability ─────────────────────────────────────────────────────────────
# Leave empty to disable Sentry (safe for local dev)
SENTRY_DSN=
VITE_SENTRY_DSN=

# ── Billing (Stripe) ──────────────────────────────────────────────────────────
# Get from: dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=sk_test_...
# Monthly subscription price ID (dashboard.stripe.com → Products → Price ID)
STRIPE_PRICE_ID=price_...
# Webhook signing secret — set the endpoint to {your-origin}/api/stripe-webhook
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Email (Resend) ────────────────────────────────────────────────────────────
# Get from: resend.com → API Keys
RESEND_API_KEY=re_...
# Verified sender domain; update after domain verification in Resend
EMAIL_FROM=Mise en Place <noreply@miseenplace.app>

# ── Initial admin account (seeded on first db:seed run) ───────────────────────
AUTH_ADMIN_EMAIL=admin@example.com
AUTH_ADMIN_PASSWORD=changeme
AUTH_ADMIN_RESTAURANT_NAME=My Restaurant
