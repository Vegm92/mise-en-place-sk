node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/coverage

# Local run artifacts (issue #295)
*.log
/screenshots/*
!/screenshots/.gitkeep
/mobile-audit/
/shots/
/.playwright-mcp/

# npm lockfile â€” this repo builds with pnpm (issue #225)
/package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor (personal, not repo config)
/.obsidian/

# Env
.env
.env.*
!.env.example
!.env.test

# SQLite databases (local dev only)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Runtime data â€” never commit (sessions contain auth tokens, uploads are user PII)
/data/sk_sessions/
/uploads/

# Project documentation (for eyes only - don't track)
CLAUDE.md
REFERENCES.md
CONTEXT.md
PRD.md
TODO.md
.scratch/
AGENTS.md
DEPLOYMENT.md
.vscode
/.claude/
/madge-out
.graphify_cached.json
/docs/*
# ...and the AI-native operating layers (added 2026-08-13) â€” tracked on purpose
!/docs/00_system/
!/docs/01_architecture/
!/docs/02_product/
!/docs/03_features/
!/docs/04_engineering/
!/docs/05_operations/
!/docs/06_decisions/
!/docs/03_features/report_templates/
!/docs/REVENUE_METRICS.md
# Architecture decision records live in docs/06_decisions/ (not ignored) â€” see its README.md
# docs/07_ai/ is internal agent ways-of-working (dev-eyes-only, like AGENTS.md/CLAUDE.md) â€” not shipped
# ...and the onboarding pack, which has to reach new teammates (tracked on purpose)
!/docs/onboarding/
!/docs/onboarding/**
# Architecture decision records live in /doc (not ignored) â€” see doc/README.md
/mise-en-place



# Synthetic invoice generator (JS port) — sources tracked, they feed the
# category-taxonomy guard and the synth:* npm scripts. Local output is not.
/synth/node_modules/
/synth/output/

# Browser QA sweep output (pnpm qa:sweep) and Playwright MCP scratch dir
/qa-report.md
/.playwright-mcp/
# Local audit screenshots (issues #657, #659) — JSON reports are tracked, screenshots are not
/shots/*.png
/shots/admin-mobile-audit-before.json
/[0-9][0-9][0-9]-*.png

# Design canvas scratch (/design skill): .dc.html sources plus a ~2MB seeded
# canvas. The deliverable is the published artifact, not a repo file.
/.design-upgrade-modal/
