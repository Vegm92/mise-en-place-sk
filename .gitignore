node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/coverage

# Local run artifacts (issue #295)
*.log
/screenshots/*
!/screenshots/.gitkeep

# npm lockfile — this repo builds with pnpm (issue #225)
/package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor (personal, not repo config)
/.obsidian/

# Env
.env
.env.*
!.env.example
!.env.test

# SQLite databases (local dev only)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Runtime data — never commit (sessions contain auth tokens, uploads are user PII)
/data/sk_sessions/
/uploads/

# Project documentation (for eyes only - don't track)
CLAUDE.md
REFERENCES.md
CONTEXT.md
PRD.md
TODO.md
/.claude/
/graphify-out
.graphify_cached.json
/docs/*
# ...and the AI-native operating layers (added 2026-08-13) — tracked on purpose
!/docs/00_system/
!/docs/01_architecture/
!/docs/02_product/
!/docs/03_features/
!/docs/04_engineering/
!/docs/05_operations/
!/docs/06_decisions/
!/docs/07_ai/
!/docs/REVENUE_METRICS.md
# Architecture decision records live in docs/06_decisions/ (not ignored) — see its README.md
# ...and the onboarding pack, which has to reach new teammates (tracked on purpose)
!/docs/onboarding/
!/docs/onboarding/**
# Architecture decision records live in /doc (not ignored) — see doc/README.md
/mise-en-place



# Synthetic invoice generator (JS port) — sources tracked, they feed the
# category-taxonomy guard and the synth:* npm scripts. Local output is not.
/synth/node_modules/
/synth/output/
