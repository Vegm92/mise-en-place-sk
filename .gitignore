node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/coverage

# Local run artifacts (issue #295)
*.log
/screenshots/*
!/screenshots/.gitkeep

# npm lockfile — this repo builds with pnpm (issue #225)
/package-lock.json

# OS
.DS_Store
Thumbs.db

# Editor (personal, not repo config)
/.obsidian/

# Env
.env
.env.*
!.env.example
!.env.test

# SQLite databases (local dev only)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Runtime data — never commit (sessions contain auth tokens, uploads are user PII)
/data/sk_sessions/
/uploads/

# Project documentation (for eyes only - don't track)
CLAUDE.md
REFERENCES.md
CONTEXT.md
PRD.md
TODO.md
/.claude/
/graphify-out
.graphify_cached.json
/docs/*
# ...except the code documentation, which replaces src/ comments (tracked on purpose)
!/docs/CODE_NOTES.md
# Architecture decision records live in /doc (not ignored) — see doc/README.md
/mise-en-place



# Synthetic generator output and virtualenv
/synth/output/
/synth/.venv/
synth/**/__pycache__/
synth/**/*.pyc

