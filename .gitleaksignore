# False positives, pinned by fingerprint (commit:file:rule:line).
# Only add entries here for findings verified NOT to be real secrets, or for
# secrets that have already been rotated and are dead in every provider.

# sonar.projectKey is SonarCloud's public project identifier, not a credential
# — visible in every SonarCloud URL for this repo.
635934f8a6c56fc9a8497e330fd10e5d58c00576:sonar-project.properties:generic-api-key:1
0577136944a1ed00e11820e97359ffd490e4ef51:sonar-project.properties:generic-api-key:1

# A fabricated Resend webhook secret in the #236 signature-verification tests:
# base64 of the ASCII string "secret-bytes-for-testing", prefixed whsec_ so the
# test exercises the route's real prefix-stripping path. Never valid at any
# provider. The literal was replaced with a runtime-built string so no future
# commit carries it, but this commit is still reachable from every PR's history
# scan, so it is pinned here.
772dc83474cab5d4bd8bbc0dd29f8715971e0d1c:tests/email-ingest-webhook.test.ts:generic-api-key:115
